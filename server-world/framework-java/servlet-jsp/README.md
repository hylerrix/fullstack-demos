# 最小可行化 Servlet 应用

```
javac -d WEB-INF/classes src/com/example/model/BeerExpert.java -Xlint:unchecked
```

```
javac -classpath /usr/local/apache-tomcat-9.0.2/lib/servlet-api.jar:WEB-INF/classes/:. -d WEB-INF/classes src/com/example/web/BeerSelect.java
```